<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zcc.highmyopia.mapper.ICheckReportsMapper">

    <insert id="insert" parameterType="com.zcc.highmyopia.po.CheckReports">
        insert into check_reports (patient_id, item_code, item_name, check_time,
                                   visit_number, create_time, update_time)
        values (#{patientId}, #{itemCode}, #{itemName}, #{checkTime}, #{visitNumber}, now(), now());
    </insert>
</mapper>
